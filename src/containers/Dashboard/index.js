import React from "react";

const Dashboard = () => {
  return (
    <div style={{ backgroundColor: "rgb(var(--bg-2dp))", width: "100%", height: "100%", borderRadius: "5px" }}>
      <h1>Dashboard</h1>
    </div>
  );
};

export default Dashboard;
